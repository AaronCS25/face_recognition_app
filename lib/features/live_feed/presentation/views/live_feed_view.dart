import 'package:flutter/material.dart';

class LiveFeedView extends StatelessWidget {
  const LiveFeedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("LiveFeedView"),);
  }
}